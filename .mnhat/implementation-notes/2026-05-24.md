### APP-AUDIT-001 / wifi_setup_screen mounted guards

- **Type:** bug-fix
- **Status:** done
- **Commit:** `ab93766 fix(app): guard wifi setup status updates after async steps`
- **Risk:** low
- **Files Changed:** `app/lib/screens/provision/wifi_setup_screen.dart`
- **Verification:** `cd app && rtk proxy flutter test`, `cd app && rtk proxy flutter analyze lib/screens/provision/wifi_setup_screen.dart`, `cd app && rtk proxy flutter analyze`

**Why:** `tmp/docs/00-app-bug-audit.md` flagged multiple `setState()` calls after awaited provisioning steps in `_provision()`. Live code still had those unguarded updates, so leaving the screen mid-flow could trigger `setState()` after dispose.

**What changed:** Added `mounted` guards immediately after each awaited provisioning step that is followed by a status `setState()`. I did not fold in the separate generic-`Exception` audit item from the same file to keep this pass limited to the confirmed crash risk.

**Tradeoff:** No narrow widget regression test was added in this pass because the screen hard-wires `BleService()` inside state, so exercising the failing async path cleanly would require extra injection/refactor beyond this finding.

**Follow-up:** `cd app && rtk proxy flutter analyze` still reports 29 pre-existing repo-wide `info` diagnostics outside this file; the touched screen itself analyzes cleanly.

### APP-AUDIT-002 / BLE notify subscription cleanup

- **Type:** bug-fix
- **Status:** done
- **Commit:** `7e768f1 fix(app): cancel BLE provisioning notify listener`
- **Risk:** low
- **Files Changed:** `app/lib/services/ble_service.dart`
- **Verification:** `cd app && rtk proxy flutter test`, `cd app && rtk proxy flutter analyze lib/services/ble_service.dart`

**Why:** `tmp/docs/00-app-bug-audit.md` flagged that `sendCredentials()` attaches a new `notifyChar.onValueReceived` listener on every provisioning attempt without storing or cancelling it. Live code still did that, so repeated provisioning could duplicate event processing and leak listeners.

**What changed:** Added a stored notify subscription, cancel any old listener before attaching a new one, and cancel it again during `disconnect()`. I left the separate `_notifyController.close()` audit item untouched for its own pass.

**Tradeoff:** No narrow regression test was added in this pass because `BleService` talks directly to `flutter_blue_plus` platform streams, so reproducing listener accumulation cleanly would require a larger seam or plugin abstraction than this focused fix warrants.

### APP-AUDIT-003 / _notifyController close on dispose

- **Type:** bug-fix
- **Status:** no-change
- **Commit:** `none — audit finding is stale for current singleton lifetime`

**Why:** The audit described `_notifyController` as leaking on every `BleService` instance lifecycle, but current code uses `factory BleService() => instance`, so the service is a singleton and I found no call sites invoking `dispose()`.

**What changed:** No code change. Closing the controller opportunistically would alter the singleton service contract without evidence of a real current bug, so I recorded this as a stale finding and moved to the next item.

### APP-AUDIT-004 / router path parameter force unwraps

- **Type:** bug-fix
- **Status:** no-change
- **Commit:** `none — route-match semantics make the audit claim too weak`

**Why:** `app/lib/core/router.dart` still uses `state.pathParameters['...']!` for required `GoRoute` segments, but the current route definitions already require those path segments for the builder to run. The available `go_router` docs also describe path-parameter parsing from matched template routes, which supports treating these values as present after a successful match.

**What changed:** No code change. Current evidence does not prove a real crash path here, so I left the required-segment unwraps alone and moved on instead of adding defensive fallback behavior without a confirmed bug.

### APP-AUDIT-005 / dashboard loading and error states

- **Type:** bug-fix
- **Status:** done
- **Commit:** `9515385 fix(app): surface dashboard loading and error states`
- **Risk:** low
- **Files Changed:** `app/lib/screens/devices/device_dashboard_screen.dart`, `app/test/device_dashboard_test.dart`
- **Verification:** `cd app && rtk proxy flutter analyze lib/screens/devices/device_dashboard_screen.dart`, `cd app && rtk proxy flutter test test/device_dashboard_test.dart`, `cd app && rtk proxy flutter test`

**Why:** `tmp/docs/00-app-bug-audit.md` was still correct here. The dashboard watched device, shadow, telemetry, and command providers through `.valueOrNull`, so initial provider failures or loading states could render misleading fallback content with no explicit dashboard status.

**What changed:** Added blocking loading and error states for initial device/shadow/telemetry bootstrap, plus explicit command-activity error text when that section fails empty. Added focused widget coverage for loading and error rendering with fake-service seams only in the test helper.

**Tradeoff:** I kept the fix local to dashboard bootstrap states instead of redesigning every non-blocking partial-data path. This addresses the confirmed silent-failure bug without changing provider contracts or adding wider state plumbing.

### APP-AUDIT-006 / provider-side ref.listen duplication

- **Type:** bug-fix
- **Status:** no-change
- **Commit:** `none — audit claim is stale under current Riverpod semantics`

**Why:** `devices_provider.dart` still calls `ref.listen(...)` inside provider `build()` methods, but current Riverpod docs explicitly show provider-side and `AsyncNotifier.build()` `ref.listen(...)` as supported usage. Current repo tests also do not expose duplicate-listener behavior from invalidation/rebuild.

**What changed:** No code change. Current evidence is too weak to justify refactoring provider listener wiring as a bug fix, so I recorded this as stale and moved on.

### APP-AUDIT-007 / root theme rebuild listener

- **Type:** bug-fix
- **Status:** done
- **Commit:** `b6127ed fix(app): rebuild theme from notifier state`
- **Risk:** low
- **Files Changed:** `app/lib/main.dart`, `app/test/widget_test.dart`
- **Verification:** `cd app && rtk proxy flutter analyze lib/main.dart`, `cd app && rtk proxy flutter test test/widget_test.dart`, `cd app && rtk proxy flutter test`

**Why:** `app/lib/main.dart` still used an empty `setState(() {})` listener on `AppState.themeMode`, so theme changes rebuilt the root stateful widget without any local state mutation.

**What changed:** Replaced the listener-driven `ConsumerStatefulWidget` root with a `ConsumerWidget` that uses `ValueListenableBuilder<ThemeMode>` around `MaterialApp.router`. Extended the smoke test to verify `AppState.themeMode` updates `MaterialApp.themeMode`.

### APP-AUDIT-008 / home screen room-name N+1 watch pattern

- **Type:** bug-fix
- **Status:** no-change
- **Commit:** `none — performance tradeoff not proven as a bug`

**Why:** `app/lib/screens/home_screen.dart` still builds room-name lookups by watching one `roomsProvider(homeId)` per distinct home in the current device list. That is a performance smell, but current evidence does not show incorrect behavior or a minimal replacement that reduces watches without broader provider reshaping.

**What changed:** No code change. I treated this as optimization advice rather than a confirmed bug and moved on to the next higher-signal finding.

### APP-AUDIT-009 / env URI validation

- **Type:** bug-fix
- **Status:** done
- **Commit:** `7baafdb fix(app): validate endpoint env values at startup`
- **Risk:** low
- **Files Changed:** `app/lib/core/env.dart`, `app/lib/core/api_client.dart`, `app/lib/main.dart`, `app/test/env_test.dart`
- **Verification:** `cd app && rtk proxy flutter analyze lib/core/env.dart lib/core/api_client.dart lib/main.dart`, `cd app && rtk proxy flutter test test/env_test.dart`, `cd app && rtk proxy flutter test`

**Why:** `app/lib/core/env.dart` still exposed raw compile-time API/MQTT endpoint strings with no validation, while `api_client.dart` consumed the API base URL directly. A malformed `--dart-define` could therefore boot the app into broken runtime behavior instead of failing fast.

**What changed:** Added explicit absolute-URI validators for API and MQTT settings, validated them at startup, and switched the API client to use the validated API URI. Added focused unit tests for valid API URLs and invalid API/MQTT settings.

**Tradeoff:** Validation currently enforces only absolute URI shape, host presence, and scheme families (`http`/`https`, `ws`/`wss`). It does not add stricter path or port policy because that would go beyond the confirmed bug.

### APP-AUDIT-010 / BLE plaintext Wi-Fi credentials

- **Type:** spec-gap
- **Status:** no-change
- **Commit:** `none — cross-stack security redesign already tracked`

**Why:** `app/lib/services/ble_service.dart` still writes SSID and password as plaintext UTF-8 over custom BLE characteristics. That is a real security gap, but fixing it requires coordinated protocol, firmware BLE security, and UX changes rather than an app-only patch.

**What changed:** No code change in this app-audit lane. Existing Beads follow-up `smart-air-vwn` already tracks the required authenticated BLE provisioning redesign, so I recorded the live gap and moved on.

### APP-AUDIT-011 / general settings room loading state

- **Type:** bug-fix
- **Status:** done
- **Commit:** `b2b1f99 fix(app): show settings room loading state`
- **Risk:** low
- **Files Changed:** `app/lib/screens/devices/settings/general_screen.dart`, `app/test/general_screen_test.dart`
- **Verification:** `cd app && rtk proxy flutter analyze lib/screens/devices/settings/general_screen.dart`, `cd app && rtk proxy flutter test test/general_screen_test.dart`, `cd app && rtk proxy flutter test`

**Why:** `general_screen.dart` already had a blocking load/error state for the device itself, so the audit wording was partially stale. The live remaining bug was narrower: the room assignment row silently fell back while `roomsProvider` was still loading.

**What changed:** Added a visible room-row loading label and spinner, and disabled room-picker taps until rooms finish loading. Added a focused widget test that holds `getRooms()` open and verifies the loading state before the room name resolves.

### APP-AUDIT-012 / screen semantics coverage

- **Type:** docs
- **Status:** no-change
- **Commit:** `none — broad accessibility pass, not one bounded bug`

**Why:** The audit claim remains directionally true: outside `device_dashboard_screen.dart` and a few atom widgets, semantics coverage is sparse across many screens. But this is a broad accessibility enhancement area spanning many files, not one surgical bugfix suitable for the one-finding-per-commit audit loop.

**What changed:** No code change. I recorded it as an over-broad enhancement item for a separate accessibility pass and moved on to the next concrete high-severity defect.

### APP-AUDIT-013 / malformed realtime occurred_at

- **Type:** bug-fix
- **Status:** done
- **Commit:** `efb5bec fix(app): guard malformed realtime timestamps`
- **Risk:** low
- **Files Changed:** `app/lib/models/realtime_event.dart`, `app/test/realtime_service_test.dart`
- **Verification:** `cd app && rtk proxy flutter analyze lib/models/realtime_event.dart`, `cd app && rtk proxy flutter test test/realtime_service_test.dart`, `cd app && rtk proxy flutter test`

**Why:** `RealtimeEvent.fromJson()` still hard-parsed `occurred_at` as a required string, so malformed or missing timestamps could crash direct decoding and caused otherwise valid SSE frames to be dropped.

**What changed:** Added a guarded `occurred_at` parse using `DateTime.tryParse`, with a deterministic epoch fallback when timestamps are missing or malformed. Added a focused SSE decoder test proving such frames no longer crash or disappear.

**Tradeoff:** The fallback timestamp is the local epoch sentinel rather than `DateTime.now()`, so bad events stay deterministic in tests and visibly distinguishable from real event times.

### APP-AUDIT-014 / malformed telemetry ts rows

- **Type:** bug-fix
- **Status:** done
- **Commit:** `0d5bae4 fix(app): ignore malformed telemetry timestamps`
- **Risk:** low
- **Files Changed:** `app/lib/models/telemetry.dart`, `app/lib/services/device_service.dart`, `app/test/device_service_test.dart`
- **Verification:** `cd app && rtk proxy flutter test test/device_service_test.dart`, `cd app && rtk proxy flutter analyze lib/models/telemetry.dart lib/services/device_service.dart`, `cd app && rtk proxy flutter test`

**Why:** `DeviceService.getTelemetry()` still mapped every API telemetry row through the generated `TelemetryPoint.fromJson()`, so one missing or malformed `ts` field could crash the whole history/live load path.

**What changed:** Added a guarded `TelemetryPoint.tryFromJson()` helper and switched telemetry fetches to drop malformed rows instead of throwing. Added a focused service test that proves valid rows still load while bad timestamps are ignored.

**Tradeoff:** Malformed telemetry rows are now skipped rather than surfaced as placeholder points, which avoids polluting charts with fake timestamps and keeps the fix app-local.

### APP-AUDIT-015 / telemetry timestamp format mismatch

- **Type:** bug-fix
- **Status:** no-change
- **Commit:** `none — server/app contract already normalizes telemetry ts to ISO strings`

**Why:** The audit claimed app expected ISO strings while MQTT telemetry uses Unix seconds, but current repo truth shows the server bridge converts stored/replayed telemetry timestamps to ISO before Flutter sees them.

**What changed:** No code change. I verified `server/api/src/services/mqtt-handlers.js` emits `payload.ts` as `new Date(...).toISOString()`, `docs/API_REFERENCE.md` documents ISO `ts` for `/api/devices/:id/telemetry` and `telemetry.point`, and `devices_provider.dart` already parses those strings.

### APP-AUDIT-016 / corrupt persisted user_json

- **Type:** bug-fix
- **Status:** done
- **Commit:** `8350d39 fix(app): clear corrupt stored user session`
- **Risk:** low
- **Files Changed:** `app/lib/core/secure_storage.dart`, `app/test/secure_storage_test.dart`
- **Verification:** `cd app && rtk proxy flutter test test/secure_storage_test.dart`, `cd app && rtk proxy flutter analyze lib/core/secure_storage.dart`, `cd app && rtk proxy flutter test`

**Why:** `SecureStorage.getUserJson()` still did `jsonDecode(raw) as Map<String, dynamic>`, so a corrupt or non-object stored session blob could crash auth restore on startup.

**What changed:** Wrapped the decode in a guard, return `null` for malformed or non-object data, and delete the bad `user_json` key so later boots stay clean. Added focused tests for malformed JSON and non-object JSON payloads.

**Tradeoff:** Corrupt persisted user state now silently clears instead of surfacing an error banner, which keeps boot resilient and matches current best-effort session restore behavior.

### APP-AUDIT-017 / BLE and provisioning test coverage

- **Type:** bug-fix
- **Status:** no-change
- **Commit:** `none — broad coverage gap, not one confirmed behavior bug`

**Why:** The audit claimed provisioning had no automated coverage, but current repo already has screen coverage for `Step1PowerOn` through `Step5Name` in `app/test/counterpart_390dp_test.dart` plus broader widget/lifecycle coverage elsewhere.

**What changed:** No code change. Remaining gaps are deeper BLE-service and end-to-end provisioning behavior tests, which are valid test-improvement work but too broad for this one-finding-at-a-time bugfix loop without a specific failing behavior to reproduce.

### APP-AUDIT-018 / auth refresh payload shape guards

- **Type:** bug-fix
- **Status:** done
- **Commit:** `255ee53 fix(app): guard malformed auth refresh payloads`
- **Risk:** low
- **Files Changed:** `app/lib/core/auth_interceptor.dart`, `app/test/auth_interceptor_test.dart`
- **Verification:** `cd app && rtk proxy flutter test test/auth_interceptor_test.dart`, `cd app && rtk proxy flutter analyze lib/core/auth_interceptor.dart`, `cd app && rtk proxy dart format --output=none --set-exit-if-changed lib/core/auth_interceptor.dart test/auth_interceptor_test.dart`, `cd app && rtk proxy flutter test`

**Why:** `AuthInterceptor._performRefresh()` still assumed `/auth/refresh` returned a map with string `accessToken` and `refreshToken`, so malformed payloads could throw runtime type errors during 401 recovery instead of logging out cleanly.

**What changed:** Added response-shape guards for refresh payloads, ignored invalid optional `refreshToken` values, and changed `_refreshFuture` cleanup so failed refreshes no longer leak unhandled side-future errors. Added focused interceptor tests for malformed refresh bodies and invalid refresh-token field types.

**Tradeoff:** Malformed refresh success payloads now collapse to forced logout rather than trying to recover partially, which is stricter but keeps session failure deterministic and safe.

### APP-AUDIT-019 / auth service response shape guards

- **Type:** bug-fix
- **Status:** done
- **Commit:** `254b2f6 fix(app): validate auth service response bodies`
- **Risk:** low
- **Files Changed:** `app/lib/services/auth_service.dart`, `app/test/auth_service_test.dart`
- **Verification:** `cd app && rtk proxy flutter test test/auth_service_test.dart`, `cd app && rtk proxy flutter analyze lib/services/auth_service.dart`, `cd app && rtk proxy dart format --output=none --set-exit-if-changed lib/services/auth_service.dart test/auth_service_test.dart`, `cd app && rtk proxy flutter test`

**Why:** `AuthService.login()`, `register()`, `refresh()`, and `_map()` still trusted success/error bodies with raw casts, so malformed payloads could throw type errors instead of consistent `ApiException` or `AuthException` results.

**What changed:** Added a shared success-body map guard, safe nested `user` extraction, ignored invalid optional `refreshToken` types, and only read `error` messages from map-shaped error bodies. Added focused tests for malformed login/register responses, invalid refresh token field types, and string error payloads.

**Tradeoff:** Malformed success bodies now fail fast as `ApiException('Unexpected server response')` instead of trying to infer partial auth state, which is stricter but keeps auth behavior predictable.

### APP-AUDIT-020 / home service response shape guards

- **Type:** bug-fix
- **Status:** done
- **Commit:** `39c2605 fix(app): validate home service response bodies`
- **Risk:** low
- **Files Changed:** `app/lib/services/home_service.dart`, `app/test/home_service_test.dart`
- **Verification:** `cd app && rtk proxy flutter test test/home_service_test.dart`, `cd app && rtk proxy flutter analyze lib/services/home_service.dart`, `cd app && rtk proxy dart format --output=none --set-exit-if-changed lib/services/home_service.dart test/home_service_test.dart`, `cd app && rtk proxy flutter test`

**Why:** `HomeService` still trusted home/room list bodies, object bodies, and error payloads with raw casts, so malformed responses could throw type errors instead of stable `ApiException` failures.

**What changed:** Added shared map and map-list guards for success bodies and limited error-message extraction to map-shaped error bodies. Added focused tests for malformed home lists, malformed room lists, malformed object bodies, and string error responses.

**Tradeoff:** Bad home-service responses now fail fast as `ApiException('Unexpected server response')` instead of attempting partial decoding, which is stricter but keeps provider state transitions predictable.

### APP-AUDIT-021 / device service response shape guards

- **Type:** bug-fix
- **Status:** done
- **Commit:** `9712cba fix(app): validate device service response bodies`
- **Risk:** low
- **Files Changed:** `app/lib/services/device_service.dart`, `app/test/device_service_response_test.dart`
- **Verification:** `cd app && rtk proxy flutter test test/device_service_response_test.dart`, `cd app && rtk proxy flutter analyze lib/services/device_service.dart`, `cd app && rtk proxy dart format --output=none --set-exit-if-changed lib/services/device_service.dart test/device_service_response_test.dart`, `cd app && rtk proxy flutter test`

**Why:** `DeviceService` still trusted several device/shadow/command success bodies and map-shaped error payloads with raw casts, so malformed responses could throw runtime type errors instead of stable `ApiException` failures.

**What changed:** Added shared map and map-list guards, centralized command-id extraction, and limited error-message extraction to map-shaped error bodies. Added focused tests for malformed provisioning, announce, device list, shadow, command, and error payload responses.

**Tradeoff:** Bad device-service payloads now fail fast as `ApiException('Unexpected server response')` rather than attempting partial decode, which is stricter but keeps downstream provider state deterministic.

### APP-AUDIT-022 / ble_service connect race guard

- **Type:** bug-fix
- **Status:** no-change
- **Commit:** `none — current callsites already serialize connect flows`

**Why:** `BleService.connect()` and `connectAndRead()` do not have an internal mutex, but current app callsites gate entry with screen-local `_connecting` / `_sending` flags and I found no live path that intentionally fires overlapping BLE connects against the singleton.

**What changed:** No code change. I did not add service-level locking without a failing repro because that would broaden singleton behavior based on a theoretical misuse path rather than a confirmed current bug.

### APP-AUDIT-023 / missing dispose overrides without owned resources

- **Type:** bug-fix
- **Status:** no-change
- **Commit:** `none — missing empty dispose() overrides is not a live leak`

**Why:** I re-checked `splash_screen.dart`, `command_history_screen.dart`, `device_chart_screen.dart`, `device_dashboard_screen.dart`, `calibration_wizard_screen.dart`, and `ui_mockup.dart`. These states currently do not own controllers, subscriptions, timers, focus nodes, or other disposable resources.

**What changed:** No code change. Adding empty `dispose()` overrides would be no-op churn, not a real fix.

### APP-AUDIT-024 / unused ble adapter subscription field

- **Type:** bug-fix
- **Status:** done
- **Commit:** `b1136a9 fix(app): remove dead ble adapter cleanup`
- **Risk:** low
- **Files Changed:** `app/lib/services/ble_service.dart`
- **Verification:** `cd app && rtk proxy flutter analyze lib/services/ble_service.dart`, `cd app && rtk proxy dart format --output=none --set-exit-if-changed lib/services/ble_service.dart`, `cd app && rtk proxy flutter test`

**Why:** `_adapterSub` was still declared and nulled in `dispose()`, but live code never assigned or used that subscription anywhere. This left dead cleanup code in the singleton BLE service and made lifecycle intent less clear.

**What changed:** Removed the unused field and its dead `dispose()` cleanup lines only. No broader BLE lifecycle refactor.

### APP-AUDIT-025 / logout invalidation of session-scoped families

- **Type:** bug-fix
- **Status:** no-change
- **Commit:** `none — current family providers auto-dispose and logout path is already covered`

**Why:** `auth_provider.dart` still invalidates only top-level session providers, but the omitted device-family providers (`shadowProvider`, `commandsProvider`, `telemetryLiveProvider`, `telemetryHistoryProvider`) are already `autoDispose`. Current `realtime_auth_lifecycle_test.dart` proves logout tears down live dashboard safely, so I do not have a live stale-state repro here.

**What changed:** No code change. Manually invalidating family instances would need a broader active-args tracking layer without evidence that current logout behavior is wrong.

### APP-AUDIT-026 / dashboard sparkline rebuild cost

- **Type:** bug-fix
- **Status:** no-change
- **Commit:** `none — small bounded transform, not confirmed bug`

**Why:** `device_dashboard_screen.dart` still rebuilds four sparkline arrays in `build()`, but each series is trimmed to at most 30 points and derived from already-loaded in-memory telemetry. I found no evidence of a user-visible regression or hot-path issue from this transform alone.

**What changed:** No code change. I treated this as a possible future optimization, not a confirmed defect for this audit loop.

### APP-AUDIT-027 / realtime reconnect backoff cancellation

- **Type:** bug-fix
- **Status:** done
- **Commit:** `aec7732 fix(app): cancel realtime reconnect backoff`
- **Risk:** low
- **Files Changed:** `app/lib/services/realtime_service.dart`, `app/test/realtime_service_test.dart`
- **Verification:** `cd app && rtk proxy flutter test test/realtime_service_test.dart`, `cd app && rtk proxy flutter analyze lib/services/realtime_service.dart test/realtime_service_test.dart`, `cd app && rtk proxy dart format --output=none --set-exit-if-changed lib/services/realtime_service.dart test/realtime_service_test.dart`, `cd app && rtk proxy flutter test`

**Why:** `watchEvents()` still slept on `Future.delayed(reconnectDelay)` after connection failures. If the provider disposed during that backoff window, stream shutdown waited for the full delay before exiting.

**What changed:** Switched reconnect waiting to `Future.any()` between delay and `cancelToken.whenCancel`, then added a focused service test proving cancellation completes promptly during backoff.

### APP-AUDIT-028 / snapshot-based list mutation overlap

- **Type:** bug-fix
- **Status:** no-change
- **Commit:** `none — theoretical overlap, no current repro`

**Why:** `homes_provider.dart` and parts of `devices_provider.dart` still rebuild state from `state.valueOrNull` snapshots after async mutations. That can be risky under overlapping writes, but I do not have repo-truth evidence of a current user-visible race in the app flows, and fixing it safely would need broader serialization or refetch policy.

**What changed:** No code change. I treated this as a concurrency hardening idea, not a confirmed current defect.

### APP-AUDIT-029 / string-based routing debt

- **Type:** bug-fix
- **Status:** no-change
- **Commit:** `none — broad maintainability debt, not bounded bug`

**Why:** The app still uses many string route literals and unnamed `GoRoute`s, but that is a repo-wide maintainability tradeoff rather than one confirmed runtime defect in current behavior.

**What changed:** No code change. A safe route-naming migration would touch many screens and router definitions, so it does not fit this one-finding audit slice.

### APP-AUDIT-030 / wifi setup generic timeout exception

- **Type:** bug-fix
- **Status:** done
- **Commit:** `985d9d5 fix(app): use typed wifi setup timeout error`
- **Risk:** low
- **Files Changed:** `app/lib/screens/provision/wifi_setup_screen.dart`
- **Verification:** `cd app && rtk proxy flutter analyze lib/screens/provision/wifi_setup_screen.dart`, `cd app && rtk proxy dart format --output=none --set-exit-if-changed lib/screens/provision/wifi_setup_screen.dart`, `cd app && rtk proxy flutter test`

**Why:** `wifi_setup_screen.dart` still threw a generic `Exception` when the provisioned device never announced online status. That was inconsistent with the app’s typed exception hierarchy and would surface a noisier generic error shape.

**What changed:** Replaced that throw with `NetworkException` while keeping the same user-facing message and flow. No broader provisioning refactor.

**Tradeoff:** I did not add a focused widget repro because `WifiSetupScreen` still hard-wires `BleService()` and multi-step provisioning side effects, so isolating just this error path would require a wider injection seam than this audit slice warrants.

### APP-AUDIT-031 / local provisioning http scheme

- **Type:** bug-fix
- **Status:** no-change
- **Commit:** `none — current LAN bootstrap tradeoff is explicit`

**Why:** `DeviceService.configureProvisionedDevice()` still talks to `http://<local-ip>/api/config`, and `ApiConfig.localProvisioningScheme` is intentionally set to `http`. That remains a cross-stack trust-model tradeoff for first-hop LAN provisioning, not an app-only defect I can safely change here.

**What changed:** No code change. A real fix needs firmware/local API transport changes, not a unilateral app patch.

### APP-AUDIT-032 / JWT expiry pre-validation

- **Type:** bug-fix
- **Status:** no-change
- **Commit:** `none — current 401→refresh flow is intentional`

**Why:** `AuthInterceptor` still sends the in-memory access token without decoding JWT `exp` client-side first. Current repo truth shows the app intentionally relies on server 401 handling plus refresh-token recovery, so this is an optimization tradeoff, not a confirmed bug.

**What changed:** No code change. Pre-validating token expiry would add duplicate client-side auth logic without evidence of broken current behavior.

### APP-AUDIT-033 / BLE provisioning substring match

- **Type:** bug-fix
- **Status:** done
- **Commit:** `4e5c3f2 fix(app): tighten ble provisioning name match`
- **Risk:** low
- **Files Changed:** `app/lib/core/app_config.dart`, `app/lib/services/ble_service.dart`, `app/test/app_config_test.dart`
- **Verification:** `cd app && rtk proxy flutter test test/app_config_test.dart`, `cd app && rtk proxy flutter analyze lib/core/app_config.dart lib/services/ble_service.dart test/app_config_test.dart`, `cd app && rtk proxy dart format --output=none --set-exit-if-changed lib/core/app_config.dart lib/services/ble_service.dart test/app_config_test.dart`, `cd app && rtk proxy flutter test`

**Why:** BLE scan logic still accepted provisioning names via substring match, so unrelated device names containing `SMART_AIR_` anywhere could false-positive as provisionable Smart Air devices.

**What changed:** Tightened provisioning-name matching to prefix semantics, reused that helper from `BleService.scan()`, and added focused unit coverage for valid prefixes versus embedded substrings.

### APP-AUDIT-034 / requestMtu result validation

- **Type:** bug-fix
- **Status:** no-change
- **Commit:** `none — negotiated MTU value is not needed for current flow`

**Why:** `flutter_blue_plus` does return the negotiated MTU, but current `BleService.connect()` already awaits `requestMtu(256)` so outright failures surface as exceptions. A smaller MTU is not itself a confirmed bug here because provisioning notify payloads are already buffered across chunks before JSON parse.

**What changed:** No code change. I did not add threshold checks without evidence that a sub-256 negotiated MTU breaks current provisioning behavior.

### FW-03-001 / inbound MQTT payload cap

- **Type:** bug-fix
- **Status:** done
- **Commit:** `none — pending finding commit`
- **Risk:** low
- **Files Changed:** `firmware/components/general/sa_mqtt/mqtt.c`, `docs/MQTT_PROTOCOL.md`, `tmp/docs/03-firmware-bug-audit.md`
- **Verification:** `. "$HOME/.espressif/v5.4.2/esp-idf/export.sh" && idf.py build`

**Why:** `mqtt_pending_rx_append()` allocated heap directly from broker-controlled `total_data_len` before any topic-specific validation. Current inbound contract payloads are all small, and OTA already has a downstream 255-byte URL bound, so the unbounded pre-parse allocation was a confirmed remote DoS vector with no functional need.

**What changed:** Added a 512-byte hard cap for current inbound MQTT topics (`command`, `shadow/get_response`, `ota/update`) before any fragment buffer allocation, rejected unexpected inbound topics in that helper, and documented the inbound-size limit in the MQTT contract plus audit note.

**Tradeoff:** Used one shared cap across the three supported inbound topics instead of three tighter per-topic numbers. That keeps the fix small while still leaving headroom above current contract shapes and OTA queue limits.

### FW-03-002 / wifi init cleanup path

- **Type:** bug-fix
- **Status:** done
- **Commit:** `none — pending finding commit`
- **Risk:** low
- **Files Changed:** `firmware/components/general/wifi/wifi.c`, `tmp/docs/03-firmware-bug-audit.md`, `.mnhat/implementation-notes/2026-05-24.md`
- **Verification:** `. "$HOME/.espressif/v5.4.2/esp-idf/export.sh" && idf.py build`

**Why:** `wifi_sta_init()` already had one shared `fail:` cleanup path that deletes the event group, unregisters handlers, and deinitializes Wi-Fi state. The lone direct `return ESP_FAIL` on `esp_netif_create_default_wifi_sta()` bypassed that cleanup and leaked the just-created event group under partial-init failure.

**What changed:** Declared `err` earlier and routed the netif-create failure through `goto fail`, preserving existing return semantics while making all post-allocation failures use the same cleanup logic.

### FW-03-003 / mqtt startup error propagation

- **Type:** bug-fix
- **Status:** done
- **Commit:** `none — pending finding commit`
- **Risk:** medium
- **Files Changed:** `firmware/components/general/sa_mqtt/mqtt.c`, `firmware/components/general/sa_mqtt/include/mqtt.h`, `tmp/docs/03-firmware-bug-audit.md`, `.mnhat/implementation-notes/2026-05-24.md`
- **Verification:** `. "$HOME/.espressif/v5.4.2/esp-idf/export.sh" && idf.py build`

**Why:** `mqtt_start()` only reported task-creation success, while `mqtt_task()` ignored failures from event registration and `esp_mqtt_client_start()`. That let boot continue with `s_client` set even if no MQTT loop was actually running.

**What changed:** Added a direct task-notification handshake so `mqtt_start()` waits for local client setup result, and made `mqtt_task()` validate `esp_mqtt_client_register_event()` plus `esp_mqtt_client_start()`, clear `s_client`, destroy the client, and return the real setup error on failure.

**Tradeoff:** `mqtt_start()` no longer returns immediately after task creation; it now blocks briefly until local MQTT setup finishes. TCP/TLS connect and broker session establishment still remain asynchronous after that point.

### FW-03-004 / bounded provisioning recv timeout

- **Type:** bug-fix
- **Status:** done
- **Commit:** `none — pending finding commit`
- **Risk:** low
- **Files Changed:** `firmware/components/general/httpd/httpd.c`, `tmp/docs/03-firmware-bug-audit.md`, `.mnhat/implementation-notes/2026-05-24.md`
- **Verification:** `. "$HOME/.espressif/v5.4.2/esp-idf/export.sh" && idf.py build`

**Why:** `config_post_handler()` retried `HTTPD_SOCK_ERR_TIMEOUT` forever with no receive budget, so a stalled provisioning client could pin the handler indefinitely during first-boot setup.

**What changed:** Added a three-timeout consecutive retry ceiling, reset that counter on forward progress, logged stalled receive state, and returned `408 Request Timeout` JSON once the bounded budget is exhausted.

### FW-03-005 / explicit provisioning recv failure response

- **Type:** bug-fix
- **Status:** done
- **Commit:** `none — pending finding commit`
- **Risk:** low
- **Files Changed:** `firmware/components/general/httpd/httpd.c`, `tmp/docs/03-firmware-bug-audit.md`, `.mnhat/implementation-notes/2026-05-24.md`
- **Verification:** `. "$HOME/.espressif/v5.4.2/esp-idf/export.sh" && idf.py build`

**Why:** Non-timeout `httpd_req_recv()` failures still bubbled out as raw `ESP_FAIL`, so the provisioning client got no structured HTTP error and the only postmortem signal was a silent connection failure.

**What changed:** Added warning logs with the partial receive state and switched that path to attempt a `400 Bad Request` JSON response instead of returning raw `ESP_FAIL`.

### FW-03-006 / local provisioning trust model

- **Type:** docs
- **Status:** done
- **Commit:** `none — pending finding commit`
- **Risk:** medium
- **Files Changed:** `docs/API_REFERENCE.md`, `docs/MQTT_PROTOCOL.md`, `docs/ARCHITECTURE.md`, `tmp/docs/03-firmware-bug-audit.md`, `.mnhat/implementation-notes/2026-05-24.md`

**Why:** Current repo truth still provisions `secret_key` over local `POST http://<device-ip>/api/config` with no TLS, request auth, or bootstrap token. That is a real exposure, but replacing it needs cross-stack protocol and product work beyond a safe firmware-only patch.

**What changed:** Left behavior unchanged and documented the actual trust assumption explicitly in architecture/API/MQTT docs, then marked the audit item as an intentional tradeoff requiring future redesign rather than pretending this audit pass fixed it.

**Follow-up:** Any real closure for this item needs a new authenticated provisioning design, likely coordinated across BLE, firmware HTTP, and app onboarding UX.

### FW-03-007 / BLE provisioning security gap

- **Type:** docs
- **Status:** blocked
- **Commit:** `none — pending finding commit`
- **Risk:** high
- **Files Changed:** `docs/API_REFERENCE.md`, `docs/ARCHITECTURE.md`, `tmp/docs/03-firmware-bug-audit.md`, `.mnhat/implementation-notes/2026-05-24.md`

**Why:** Firmware BLE provisioning still accepts SSID/password writes on plain writable characteristics, and the current Flutter onboarding flow does not implement any pairing or secure-session step. Enforcing BLE encryption/MITM in firmware alone would change onboarding UX and can strand the current app flow.

**What changed:** Left behavior unchanged, documented the current radio-range trust assumption explicitly, and marked the audit item blocked pending a coordinated secure-BLE redesign instead of applying an unverified firmware-only lockout.

**Follow-up:** Real closure needs product choice on pairing UX plus matching app and firmware changes for bond/encryption handling.

### APP-AUDIT-035 / malformed provisioning query params

- **Type:** bug-fix
- **Status:** done
- **Commit:** `b74ba81 fix(app): guard malformed provisioning routes`
- **Risk:** low
- **Files Changed:** `app/lib/screens/provision/step4_cloud.dart`, `app/lib/screens/provision/step5_name.dart`, `app/test/provision_route_guard_test.dart`
- **Verification:** `cd app && rtk proxy flutter test test/provision_route_guard_test.dart`, `cd app && rtk proxy flutter analyze lib/screens/provision/step4_cloud.dart lib/screens/provision/step5_name.dart test/provision_route_guard_test.dart`, `cd app && rtk proxy dart format --output=none --set-exit-if-changed lib/screens/provision/step4_cloud.dart lib/screens/provision/step5_name.dart test/provision_route_guard_test.dart`, `cd app && rtk proxy flutter test`

**Why:** Provisioning routes still defaulted missing query params to empty strings. Live code let `Step4CloudScreen` poll with missing identifiers and let `Step5NameScreen` derive a device-name suffix from `deviceId` immediately, which made malformed deep-link entry unsafe.

**What changed:** Added explicit invalid-route states in Step 4 and Step 5 so malformed provisioning entry shows a restart message instead of polling or crashing. Added focused widget coverage for both invalid-entry screens.

### APP-AUDIT-036 / files exceeding 500 lines

- **Type:** refactor
- **Status:** no-change
- **Commit:** `none — file length alone is not a confirmed app bug`

**Why:** Current repo still has several oversized app files, but the audit evidence is partly stale on exact paths: `app/lib/screens/profile/home_detail_screen.dart` is 597 lines, `app/lib/screens/ble_scan_screen.dart` is 578, `app/lib/providers/devices_provider.dart` is 557, `app/lib/widgets/add_device_sheet.dart` is 543, and `app/lib/screens/devices/device_dashboard_screen.dart` is now 661.

**What changed:** No code change. Splitting these files would be a broad refactor with behavior-risk and no reproduced defect, so I recorded this as maintainability debt instead of forcing a scope-expanding rewrite inside the bug-fix loop.

### APP-AUDIT-037 / build methods exceeding 200 lines

- **Type:** refactor
- **Status:** no-change
- **Commit:** `none — oversized build methods are debt, not reproduced breakage`

**Why:** Current repo still has large screen builds in `device_dashboard_screen.dart`, `general_screen.dart`, and `home_detail_screen.dart`, but the audit is already stale on at least one exact claim because `profile_screen.dart` now contains smaller split builds instead of one 177-line monolith.

**What changed:** No code change. Extracting these remaining screen sections would be a broad UI refactor without a failing behavior, so I kept the audit loop focused on confirmed bugs instead of structure-only cleanup.

### APP-AUDIT-038 / no tests for core infrastructure

- **Type:** bug-fix
- **Status:** no-change
- **Commit:** `none — current coverage already closes most of this gap`

**Why:** This audit claim is stale for current repo state. `app/test/env_test.dart`, `app/test/auth_interceptor_test.dart`, and `app/test/widget_test.dart` now provide direct coverage for `Env`, `AuthInterceptor`, and `AppState.themeMode`, leaving only `app_exception.dart` as an indirect-only helper rather than an untested runtime subsystem.

**What changed:** No code change. I did not add tests just to satisfy an old broad audit statement because the reported zero-coverage premise is no longer true and `app_exception.dart` has no reproduced behavior bug to drive a focused spec.

### APP-AUDIT-039 / pumpAndSettle flakiness risk

- **Type:** bug-fix
- **Status:** no-change
- **Commit:** `none — generic flake warning without reproduced failing test`

**Why:** Current test suite still uses `pumpAndSettle()` in multiple widget tests, but the repo already documents and isolates known continuous-animation cases in `app/test/goldens/README.md` with deterministic `customPump` hooks. I found no current failing test or timeout evidence tied to the remaining `pumpAndSettle()` calls.

**What changed:** No code change. Replacing every remaining `pumpAndSettle()` call blindly would be speculative churn, so I left this as a test-maintenance note until a concrete flaky case is reproduced.

### APP-AUDIT-040 / BleService singleton API inconsistency

- **Type:** refactor
- **Status:** blocked
- **Commit:** `none — Dart GitNexus impact unavailable for safe code edit`

**Why:** Repo truth confirms the inconsistency: `BleService` has a private constructor plus singleton factory, but its class comment still says non-singleton instances are allowed. Small comment cleanup would be safe in isolation, but the goal rules require GitNexus impact before touching app symbols.

**What changed:** No code change. I rebuilt `tree-sitter-dart` and forced `gitnexus analyze`, but GitNexus still skipped Dart files and `impact BleService` could not resolve the symbol, so I treated the cleanup as blocked by tooling rather than bypassing the pre-edit safety rule.

### SYNC-AUDIT-001 / desired shadow contract

- **Type:** bug-fix
- **Status:** done
- **Commit:** `c1460a4 fix(server): restrict desired shadow to supported keys`
- **Risk:** medium
- **Files Changed:** `server/api/src/routes/shadow.js`, `server/api/test/shadow-route-validation.test.js`, `docs/API_REFERENCE.md`, `docs/MQTT_PROTOCOL.md`
- **Verification:** `cd server/api && rtk node --test test/shadow-route-validation.test.js`, `cd server/api && rtk node --check src/routes/shadow.js`, `rtk git diff --check -- server/api/src/routes/shadow.js server/api/test/shadow-route-validation.test.js docs/API_REFERENCE.md docs/MQTT_PROTOCOL.md`

**Why:** Audit finding 1 was still real in live code: API rejected `mode` and `relay_*` but accepted arbitrary desired keys, while firmware only applies `mode` and `relay_*` from `shadow/get_response`.

**What changed:** Narrowed desired-shadow route contract to firmware-supported keys only, added route coverage for accept/reject behavior, and synced API/MQTT docs to describe desired shadow as declarative target state for `mode` and relay keys.

### SYNC-AUDIT-002 / provisioning trust chain

- **Type:** spec-gap
- **Status:** no-change
- **Commit:** `none — design-level follow-up already tracked`

**Why:** Audit finding 2 is still current in live code: BLE provisioning lacks visible auth hardening, local `POST /api/config` remains plain HTTP bootstrap, and MQTT `secret_key` is still stored plaintext in NVS.

**What changed:** No narrow fix applied in this goal slice because the finding spans firmware security config, local bootstrap trust model, storage/encryption strategy, and app provisioning flow. Existing tracked follow-ups already cover the real work: `smart-air-vwn`, `smart-air-moo`, `smart-air-20p`.

### SYNC-AUDIT-003 / duplicate reconnect shadow publish

- **Type:** bug-fix
- **Status:** done
- **Commit:** `0f38191 fix(server): remove duplicate shadow sync on reconnect`
- **Risk:** low
- **Files Changed:** `server/api/src/services/mqtt-handlers.js`, `server/api/test/mqtt-handlers.test.js`, `docs/API_REFERENCE.md`, `docs/MQTT_PROTOCOL.md`
- **Verification:** `cd server/api && rtk node --test test/mqtt-handlers.test.js`, `cd server/api && rtk node --check src/services/mqtt-handlers.js`, `rtk git diff --check -- server/api/src/services/mqtt-handlers.js server/api/test/mqtt-handlers.test.js docs/API_REFERENCE.md docs/MQTT_PROTOCOL.md`

**Why:** Audit finding 3 was still real: firmware already sends `shadow/get` after MQTT reconnect, but server also pushed desired shadow on `status online`, causing duplicate desired-state publish/apply cycles.

**What changed:** Removed status-triggered desired-shadow publish so reconnect bootstrap now has one owner: device `shadow/get`. Synced server tests and docs to describe `handleStatus()` as announce/flush only.

### SYNC-AUDIT-004 / MQTT command_id dedupe

- **Type:** bug-fix
- **Status:** done
- **Commit:** `c84a066 fix(firmware): dedupe repeated mqtt commands`
- **Risk:** medium
- **Files Changed:** `firmware/components/general/sa_mqtt/mqtt.c`, `docs/MQTT_PROTOCOL.md`, `docs/API_REFERENCE.md`
- **Verification:** `rtk proxy zsh -lc '. $HOME/.espressif/v5.4.2/esp-idf/export.sh >/dev/null 2>&1 && cd firmware && idf.py build'`, `rtk git diff --check -- firmware/components/general/sa_mqtt/mqtt.c docs/MQTT_PROTOCOL.md docs/API_REFERENCE.md`, `gitnexus detect_changes` staged scope low risk

**Why:** Audit finding 4 was still real: server sends QoS1 commands from a DB-backed pending queue, but firmware executed incoming command payloads directly and only used `command_id` for ack publishing, so MQTT redelivery could rerun side effects like calibration.

**What changed:** Added a 20-entry RAM `command_id` cache in firmware so duplicate commands are ignored while the original is pending and reuse the cached terminal `done|error` ack after completion; synced MQTT/API docs to describe the implemented boot-lifetime limits.

**Tradeoff:** Dedupe is intentionally RAM-only to stay narrow and safe for this pass, so reboot or explicit MQTT client restart still clears the cache and can allow a replayed command to run again.

### SYNC-AUDIT-005 / shadow contract doc drift

- **Type:** docs
- **Status:** no-change
- **Commit:** `none — finding is stale after SYNC-AUDIT-001 and SYNC-AUDIT-003 updates`

**Why:** The audit's last P3 item claimed `docs/MQTT_PROTOCOL.md` still showed unsupported `desired.power` and numeric `mode`, but current repo docs now show only `mode` plus `relay_1..3`, matching both firmware shadow apply logic and the server desired-shadow validator.

**What changed:** No code or docs change. I re-checked current `docs/MQTT_PROTOCOL.md`, `docs/API_REFERENCE.md`, `server/api/src/routes/shadow.js`, and `firmware/components/core/sysload/sysload.c`, and the documented shadow contract is aligned now.

### SYNC-AUDIT-01-P1 / future shadow timestamp skew

- **Type:** bug-fix
- **Status:** done
- **Commit:** `75569d9 fix(server): clamp future shadow report ts`
- **Risk:** low
- **Files Changed:** `server/api/src/services/shadow.js`, `server/api/test/shadow-cache.test.js`
- **Verification:** `cd server/api && rtk proxy npm test -- test/shadow-cache.test.js test/mqtt-handlers.test.js`

**Why:** Round-01 sync audit was correct: `updateReported()` used raw client `payload.ts` for the shadow UPSERT and monotonic stale check, so one future-dated `shadow/report` could push `reported.ts` far ahead and freeze later real reports as stale.

**What changed:** Normalized only future shadow timestamps inside `updateReported()` before both storage and ordering. I explicitly left very old timestamps unchanged, because clamping them upward would let stale reports bypass the existing stale guard.

### SYNC-AUDIT-01-P2A / firmware version persistence

- **Type:** bug-fix
- **Status:** done
- **Commit:** `046d92e fix(server): persist firmware version from status`
- **Risk:** low
- **Files Changed:** `server/api/src/services/mqtt-handlers.js`, `server/api/test/mqtt-handlers.test.js`
- **Verification:** `cd server/api && rtk proxy npm test -- test/mqtt-handlers.test.js`

**Why:** Round-01 sync audit was correct: `handleStatus()` exposed `payload.firmware` in realtime events, but durable `devices.firmware_ver` stayed stale because status writes only updated `online` and `last_seen`.

**What changed:** Reused one parsed `firmwareVersion` value in `handleStatus()` and persisted it with `COALESCE($2, firmware_ver)` so online payloads update stored firmware while LWT or other status payloads that omit firmware do not erase it.

### SYNC-AUDIT-01-P2B / desired shadow mode-relay invariant

- **Type:** bug-fix
- **Status:** done
- **Commit:** `6c9da09 fix(server): reject impossible desired shadow relay state`
- **Risk:** medium
- **Files Changed:** `server/api/src/routes/shadow.js`, `server/api/test/shadow-route-validation.test.js`, `docs/API_REFERENCE.md`, `docs/MQTT_PROTOCOL.md`
- **Verification:** `cd server/api && rtk proxy npm test -- test/shadow-route-validation.test.js`

**Why:** Round-01 sync audit was correct: API accepted desired relay-on states that firmware cannot apply while effective mode is `off`, so desired shadow could diverge permanently from reported state.

**What changed:** Added route-level invariant validation that resolves effective mode as `body.mode` → stored desired mode → current reported mode, then rejects any `relay_N=true` while that mode is `off`. Updated API/MQTT docs to document the rejection rule.

### SYNC-AUDIT-01-P2C / reconnect relay shadow snapshot

- **Type:** bug-fix
- **Status:** done
- **Commit:** `87afad9 fix(firmware): republish relay shadow on reconnect`
- **Risk:** medium
- **Files Changed:** `firmware/components/core/device_mode/include/device_mode.h`, `firmware/components/core/device_mode/device_mode.c`, `firmware/components/general/sa_mqtt/mqtt.c`, `firmware/components/general/sa_mqtt/CMakeLists.txt`, `docs/MQTT_PROTOCOL.md`
- **Verification:** `rtk proxy zsh -lc '. $HOME/.espressif/v5.4.2/esp-idf/export.sh >/dev/null 2>&1 && cd firmware && idf.py build'`

**Why:** Round-01 sync audit was correct: relays restore from NVS locally, but reconnect path only sent `status` and `shadow/get`, so server could keep stale relay state when no later relay delta or mode change happened.

**What changed:** Added `device_mode_publish_current_shadow()` and called it on `MQTT_EVENT_CONNECTED` before `shadow/get`, so firmware republishes one full current mode/relay shadow snapshot on reconnect. Also added the missing `sa_mqtt -> device_mode` component dependency required for that hook to compile.

### SYNC-AUDIT-01-P3 / status payload doc drift

- **Type:** docs
- **Status:** done
- **Commit:** `6dd514c docs(firmware): align mqtt status payload examples`
- **Files Changed:** `docs/MQTT_PROTOCOL.md`
- **Verification:** `rtk sed -n '54,78p' docs/MQTT_PROTOCOL.md`, `rtk sed -n '420,438p' firmware/components/general/sa_mqtt/mqtt.c`, `rtk sed -n '136,165p' server/api/src/services/mqtt-handlers.js`

**Why:** Round-01 P3 was still real: docs showed `ip` and `ts` in MQTT status payloads even though live firmware publishes only `online` plus optional `firmware`, and server only consumes that shape.

**What changed:** Updated the status examples and notes in `docs/MQTT_PROTOCOL.md` to match current online and offline payloads exactly. No code change was needed.
